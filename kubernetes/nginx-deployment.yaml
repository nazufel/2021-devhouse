apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: devhouse
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx:1.21
          ports:
            - containerPort: 80
      #     volumeMounts:
      #       - name: config
      #         mountPath: "/etc/nginx"
      #         readOnly: true
      #       - name: index
      #         mountPath: "/www/index.html"
      #         readOnly: true
      # volumes:
      #   - name: config
      #     configMap:
      #       name: nginx-config
      #   - name: index
      #     configMap:
      #       name: nginx-index
#EOF